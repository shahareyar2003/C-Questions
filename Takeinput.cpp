#include<iostream>
using namespace std;
int main(){
    string name;
    // The Operator used for taking the input is known as the extraction (>>)
    // The Operator used for displaying the output is known as the insertion (<<)
    cout<<"Enter your name: ";
    cin>>name;
    cout<<"Your name is "<<name;
    return 0;
}